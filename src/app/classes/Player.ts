import { Card } from './Card';

export class Player{
	playerprize = null;
	playerId = null;
	playerActivePkm = Card;
	//we'll replace this property with the rest of the states required, for now we'll use a sample card moving around screen and keep
	//track of turn only for proof of concept

	

}
